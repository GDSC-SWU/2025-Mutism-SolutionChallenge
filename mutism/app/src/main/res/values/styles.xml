<resources>
    <style name="CustomChip" parent="@style/Widget.MaterialComponents.Chip.Filter">
        <item name="chipBackgroundColor">@color/chip_bg_color_selector</item>
        <item name="chipStrokeColor">@color/chip_stroke_selector</item>
        <item name="chipStrokeWidth">1dp</item>

        <item name="android:textColor">@color/chip_text_color_selector</item>
        <item name="checkedIcon">@drawable/tac_checked</item>

        <item name="closeIcon">@drawable/chip_cancel</item>
        <item name="closeIconTint">@color/white</item>
        <item name="closeIconSize">14dp</item>
        <item name="paddingStart">10dp</item>
        <item name="paddingEnd">10dp</item>
    </style>
</resources>
